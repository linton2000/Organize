# Compose file for local testing purposes
services:
  # Django API service
  api:
    container_name: api-cntr
    restart: always
    build:
      context: ./backend
      dockerfile: Containerfile
    ports:
      - 8000:8000
      